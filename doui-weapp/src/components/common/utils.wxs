/*
  @function 处理从父类传入的尺寸单位
  @return String
*/
var unitTranslateHandler = function (unit) {
  if (getRegExp('^\d+$').test(unit)) 
    return unit + 'px'
  return unit
}

module.exports = {
  unitTranslateHandler: unitTranslateHandler
}